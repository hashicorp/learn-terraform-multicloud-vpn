version: '3.8'
services:
  frontend:
    image: 'hashicorpdemoapp/frontend:v0.0.8'
    volumes:
      - type: bind
        source: ./nginx.conf
        target: /etc/nginx/conf.d/default.conf
    ports:
      - '3000:3000'
